---
date: 2026-08-12
---

# 2026-08-12 工作日志

| 类型 | 内容 | 涉及文件 | 备注 |
| --- | --- | --- | --- |
| 修复 | 更换 AISStream API Key（旧 Key 被 429 限流，默认 Key 替换为新 Key，仍支持环境变量 AISSTREAM_API_KEY 覆盖） | backend/src/main/resources/application.yml | 本机网络拦截 wss，且 AISStream 对当前出口 IP 用户级限流（429），需换网络/服务器验证 |
| 修复 | 新增 aisstream.enabled 配置开关（默认 false），可整体停用 AISStream 连接与自动重连，避免继续触发 429 限流 | backend/src/main/java/com/freight/service/impl/AisStreamServiceImpl.java、backend/src/main/resources/application.yml | 恢复时改为 true 或设环境变量 AISSTREAM_ENABLED=true |

---
date: 2026-08-13
---

# 2026-08-13 工作日志

| 类型 | 内容 | 涉及文件 | 备注 |
| --- | --- | --- | --- |
| 优化 | 订单后端校验加固：SO号必填、运输方式限 SEA/AIR/LAND、各字符串字段按表结构加长度上限（SO号/起运地/目的地/货物名称/船名航次/船公司 100，贸易方式 20，柜封号 200，备注 500） | backend/src/main/java/com/freight/dto/FreightOrderDTO.java | 长度与 V20260728 迁移脚本的列定义一致 |
| 优化 | 新建订单接口返回完整新订单（原返回空），前端创建成功后直接打开详情抽屉，可立即上传附件 | FreightOrderService.java、FreightOrderServiceImpl.java、FreightOrderController.java、frontend/src/views/order/OrderView.vue | 接口返回结构变化：POST /api/orders 返回 Result<FreightOrder> |
| 优化 | 订单导出 CSV 补充"客户"列（SO号之后） | frontend/src/views/order/OrderView.vue | 客户名按 customerId 从已加载列表解析 |
| 优化 | 编辑弹窗附件由双击预览改为单击预览，并在附件区增加操作提示 | frontend/src/views/order/OrderView.vue | — |
| 优化 | 订单表单保存时仅提交后端 DTO 定义字段，不再携带表格行展开的 status/createTime 等冗余字段 | frontend/src/views/order/OrderView.vue | 新增 buildOrderPayload 统一构造 |
| 优化 | 前端去重与缓存：查询参数拼装抽 buildQueryParams（列表/导出共用）、附件预览抽 previewOrderAttachment（编辑弹窗/详情抽屉共用）、客户列表 60 秒缓存（新建订单时强制刷新） | frontend/src/views/order/OrderView.vue | mvn test 5/5 通过，npm run build 通过，后端已重启 |
| 修复 | 订单编辑弹窗件数字段校验：仅允许正整数（不允许小数与 0），与后端 Integer 类型对齐 | frontend/src/views/order/OrderView.vue | 新增 positiveIntegerValidator |
| 修复 | 打开订单弹窗时清除 el-form 复用的残留校验提示（clearValidate） | frontend/src/views/order/OrderView.vue | 修复上次校验失败红字残留 |
| 修复 | 客户数超过下拉 999 条分页上限时，编辑订单自动补拉当前客户进选项，避免 el-select 显示客户 ID | frontend/src/views/order/OrderView.vue | 新增 ensureCustomerLoaded，按 id 单独补拉 |
| 文档 | O6 结论：删除订单保留磁盘附件为有意设计（逻辑删除可恢复），在 delete() 补充注释说明；既有测试 deletingOrderKeepsAttachmentsForLogicalDeletion 锁定该行为 | backend/src/main/java/com/freight/service/impl/FreightOrderServiceImpl.java | 曾尝试删除时清理目录，因与既有测试断言冲突回退，改为注释说明 |
| 修复 | B6 新建/修改订单增加客户存在性校验（排除已逻辑删除客户），无效 customerId 直接拒绝，避免脏数据订单与附件功能不可用 | backend/src/main/java/com/freight/service/impl/FreightOrderServiceImpl.java、backend/src/test/java/com/freight/service/impl/FreightOrderServiceImplSecurityTest.java | 新增 requireExistingCustomer；补 createOrderRequiresExistingCustomer、updateOrderRequiresExistingCustomer 两个测试，mvn test 7/7 通过，后端已重启 |
| 调整 | B6 客户存在性校验收敛到新建订单：修改订单不再校验（客户被删除后历史订单仍可编辑，与附件目录 selectByIdIncludeDeleted 兜底设计一致）；测试改为 updateOrderAllowsMissingCustomer | backend/src/main/java/com/freight/service/impl/FreightOrderServiceImpl.java、backend/src/test/java/com/freight/service/impl/FreightOrderServiceImplSecurityTest.java | mvn test 7/7 通过，后端已重启 |
| 测试 | 全站性能测试与压测：单接口基准、订单列表阶梯加压（c=1→100）、混合业务场景（c=20/50）、60 秒持续稳定性、前端 dev/生产构建对比；结论：混合场景约 1200 rps 平台期、50 并发零错误 p99<80ms、JVM 内存 254MB 无泄漏；发现 /v3/api-docs 每次请求返回 500 的既有问题 | 压测脚本 /tmp/abtest.sh、/tmp/mixload.mjs | 工具 ab + 自研 Node 并发脚本；用应用 JWT 密钥签发测试 token，未改数据库；写接口未测避免污染数据 |
| 修复 | Swagger 不可用：springdoc-openapi 2.0.4 与 Spring Boot 3.5（Spring 6.2）不兼容，调用已移除的 ControllerAdviceBean 构造器导致 /v3/api-docs 每次请求抛 NoSuchMethodError（被全局异常包装成 500，仅 73 rps）；升级 springdoc 至 2.8.17 | backend/pom.xml | 修复后 /v3/api-docs 返回 47 个接口的 OpenAPI 3.1 文档、swagger-ui.html 200，压测 1807 rps（25 倍提升）；mvn test 7/7 通过，后端已重启 |
| 修复 | A1 单设备登录失效：新登录时只删了指针 key 未删旧 refresh token 本体，旧设备 30 天内仍可刷新；现改为先读指针再删旧 token 本体，refresh 重放检测分支同步吊销当前有效 token | backend/src/main/java/com/freight/service/impl/AuthServiceImpl.java | 端到端实测：二次登录后旧 token 刷新返回 401，重放旧 token 触发全部吊销，正常刷新轮转不受影响 |
| 修复 | A3 注册并发竞态：相同用户名同时注册都通过查重后撞唯一索引报 500；现捕获 DuplicateKeyException 转"该账号已被注册" | backend/src/main/java/com/freight/service/impl/AuthServiceImpl.java | 新增 AuthServiceImplTest 2 个用例 |
| 文档 | A4 注册查重包含已删除账号属有意设计：username 唯一索引限制，已删除账号仍占用用户名；代码补注释说明，复用已删除用户名需先调整表结构 | backend/src/main/java/com/freight/service/impl/AuthServiceImpl.java | 如需复用需改 uk_username 索引方案，待决策 |
